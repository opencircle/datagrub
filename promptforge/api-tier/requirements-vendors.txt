# Vendor evaluation libraries - optimized for faster builds
# These are installed separately to avoid dependency conflicts

# Core vendor libraries (required)
deepeval==3.6.2
ragas==0.3.6
mlflow==3.4.0

# Optional vendor libraries
# deepchecks-llm-client>=0.1.0  # Managed service, install separately if needed
# arize-phoenix>=3.0.0  # Large dependency tree, install separately if needed

# Support libraries
datasets==2.19.0
nest-asyncio==1.6.0
textstat==0.7.3
rouge-score==0.1.2
tiktoken==0.8.0
nltk==3.9.1
sqlparse==0.5.3
